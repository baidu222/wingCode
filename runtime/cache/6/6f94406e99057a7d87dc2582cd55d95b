{"content":{"cover_id":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"cover_id\" value=\"0\"  id=\"cover_id_cover_id\"  />\n    <div id=\"fileList_cover_id\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_cover_id\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_cover_id = $('#fileList_cover_id'),\n        // 优化retina, 在retina下这个值是2\n        ratio_cover_id = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_cover_id = 100 * ratio_cover_id,\n    thumbnailHeight_cover_id = 100 * ratio_cover_id,\n\n    // Web Uploader实例\n    uploader_cover_id;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_cover_id = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_cover_id',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_cover_id.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_cover_id.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_cover_id.html( $li );\n\n        // 创建缩略图\n        uploader_cover_id.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_cover_id, thumbnailHeight_cover_id );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_cover_id.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_cover_id.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_cover_id\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_cover_id.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_cover_id.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","pic":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"pic\" value=\"0\"  id=\"cover_id_pic\"  />\n    <div id=\"fileList_pic\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_pic\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_pic = $('#fileList_pic'),\n        // 优化retina, 在retina下这个值是2\n        ratio_pic = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_pic = 100 * ratio_pic,\n    thumbnailHeight_pic = 100 * ratio_pic,\n\n    // Web Uploader实例\n    uploader_pic;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_pic = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_pic',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_pic.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic.html( $li );\n\n        // 创建缩略图\n        uploader_pic.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_pic, thumbnailHeight_pic );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_pic.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_pic.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_pic\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_pic.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_pic.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","logo":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"logo\" value=\"0\"  id=\"cover_id_logo\"  />\n    <div id=\"fileList_logo\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_logo\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_logo = $('#fileList_logo'),\n        // 优化retina, 在retina下这个值是2\n        ratio_logo = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_logo = 100 * ratio_logo,\n    thumbnailHeight_logo = 100 * ratio_logo,\n\n    // Web Uploader实例\n    uploader_logo;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_logo = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_logo',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_logo.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_logo.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_logo.html( $li );\n\n        // 创建缩略图\n        uploader_logo.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_logo, thumbnailHeight_logo );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_logo.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_logo.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_logo\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_logo.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_logo.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","icon":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"icon\" value=\"\"  id=\"cover_id_icon\"  />\n    <div id=\"fileList_icon\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_icon\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_icon = $('#fileList_icon'),\n        // 优化retina, 在retina下这个值是2\n        ratio_icon = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_icon = 100 * ratio_icon,\n    thumbnailHeight_icon = 100 * ratio_icon,\n\n    // Web Uploader实例\n    uploader_icon;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_icon = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_icon',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_icon.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_icon.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_icon.html( $li );\n\n        // 创建缩略图\n        uploader_icon.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_icon, thumbnailHeight_icon );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_icon.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_icon.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_icon\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_icon.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_icon.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","logo2":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"logo2\" value=\"0\"  id=\"cover_id_logo2\"  />\n    <div id=\"fileList_logo2\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_logo2\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_logo2 = $('#fileList_logo2'),\n        // 优化retina, 在retina下这个值是2\n        ratio_logo2 = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_logo2 = 100 * ratio_logo2,\n    thumbnailHeight_logo2 = 100 * ratio_logo2,\n\n    // Web Uploader实例\n    uploader_logo2;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_logo2 = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_logo2',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_logo2.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_logo2.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_logo2.html( $li );\n\n        // 创建缩略图\n        uploader_logo2.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_logo2, thumbnailHeight_logo2 );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_logo2.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_logo2.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_logo2\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_logo2.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_logo2.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","pic1":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"pic1\" value=\"0\"  id=\"cover_id_pic1\"  />\n    <div id=\"fileList_pic1\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_pic1\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_pic1 = $('#fileList_pic1'),\n        // 优化retina, 在retina下这个值是2\n        ratio_pic1 = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_pic1 = 100 * ratio_pic1,\n    thumbnailHeight_pic1 = 100 * ratio_pic1,\n\n    // Web Uploader实例\n    uploader_pic1;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_pic1 = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_pic1',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic1.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_pic1.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic1.html( $li );\n\n        // 创建缩略图\n        uploader_pic1.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_pic1, thumbnailHeight_pic1 );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_pic1.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_pic1.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_pic1\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_pic1.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_pic1.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","pic2":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"pic2\" value=\"0\"  id=\"cover_id_pic2\"  />\n    <div id=\"fileList_pic2\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_pic2\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_pic2 = $('#fileList_pic2'),\n        // 优化retina, 在retina下这个值是2\n        ratio_pic2 = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_pic2 = 100 * ratio_pic2,\n    thumbnailHeight_pic2 = 100 * ratio_pic2,\n\n    // Web Uploader实例\n    uploader_pic2;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_pic2 = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_pic2',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic2.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_pic2.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic2.html( $li );\n\n        // 创建缩略图\n        uploader_pic2.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_pic2, thumbnailHeight_pic2 );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_pic2.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_pic2.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_pic2\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_pic2.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_pic2.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","pic3":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"pic3\" value=\"0\"  id=\"cover_id_pic3\"  />\n    <div id=\"fileList_pic3\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_pic3\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_pic3 = $('#fileList_pic3'),\n        // 优化retina, 在retina下这个值是2\n        ratio_pic3 = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_pic3 = 100 * ratio_pic3,\n    thumbnailHeight_pic3 = 100 * ratio_pic3,\n\n    // Web Uploader实例\n    uploader_pic3;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_pic3 = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_pic3',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic3.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_pic3.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic3.html( $li );\n\n        // 创建缩略图\n        uploader_pic3.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_pic3, thumbnailHeight_pic3 );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_pic3.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_pic3.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_pic3\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_pic3.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_pic3.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","pic4":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"pic4\" value=\"0\"  id=\"cover_id_pic4\"  />\n    <div id=\"fileList_pic4\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_pic4\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_pic4 = $('#fileList_pic4'),\n        // 优化retina, 在retina下这个值是2\n        ratio_pic4 = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_pic4 = 100 * ratio_pic4,\n    thumbnailHeight_pic4 = 100 * ratio_pic4,\n\n    // Web Uploader实例\n    uploader_pic4;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_pic4 = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_pic4',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic4.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_pic4.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic4.html( $li );\n\n        // 创建缩略图\n        uploader_pic4.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_pic4, thumbnailHeight_pic4 );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_pic4.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_pic4.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_pic4\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_pic4.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_pic4.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","pic5":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"pic5\" value=\"0\"  id=\"cover_id_pic5\"  />\n    <div id=\"fileList_pic5\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_pic5\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_pic5 = $('#fileList_pic5'),\n        // 优化retina, 在retina下这个值是2\n        ratio_pic5 = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_pic5 = 100 * ratio_pic5,\n    thumbnailHeight_pic5 = 100 * ratio_pic5,\n\n    // Web Uploader实例\n    uploader_pic5;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_pic5 = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_pic5',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic5.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_pic5.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic5.html( $li );\n\n        // 创建缩略图\n        uploader_pic5.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_pic5, thumbnailHeight_pic5 );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_pic5.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_pic5.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_pic5\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_pic5.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_pic5.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","pic6":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"pic6\" value=\"0\"  id=\"cover_id_pic6\"  />\n    <div id=\"fileList_pic6\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_pic6\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_pic6 = $('#fileList_pic6'),\n        // 优化retina, 在retina下这个值是2\n        ratio_pic6 = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_pic6 = 100 * ratio_pic6,\n    thumbnailHeight_pic6 = 100 * ratio_pic6,\n\n    // Web Uploader实例\n    uploader_pic6;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_pic6 = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_pic6',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic6.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_pic6.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic6.html( $li );\n\n        // 创建缩略图\n        uploader_pic6.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_pic6, thumbnailHeight_pic6 );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_pic6.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_pic6.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_pic6\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_pic6.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_pic6.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","pic7":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"pic7\" value=\"0\"  id=\"cover_id_pic7\"  />\n    <div id=\"fileList_pic7\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_pic7\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_pic7 = $('#fileList_pic7'),\n        // 优化retina, 在retina下这个值是2\n        ratio_pic7 = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_pic7 = 100 * ratio_pic7,\n    thumbnailHeight_pic7 = 100 * ratio_pic7,\n\n    // Web Uploader实例\n    uploader_pic7;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_pic7 = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_pic7',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic7.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_pic7.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_pic7.html( $li );\n\n        // 创建缩略图\n        uploader_pic7.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_pic7, thumbnailHeight_pic7 );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_pic7.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_pic7.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_pic7\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_pic7.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_pic7.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","license":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"license\" value=\"0\"  id=\"cover_id_license\"  />\n    <div id=\"fileList_license\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_license\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_license = $('#fileList_license'),\n        // 优化retina, 在retina下这个值是2\n        ratio_license = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_license = 100 * ratio_license,\n    thumbnailHeight_license = 100 * ratio_license,\n\n    // Web Uploader实例\n    uploader_license;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_license = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_license',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_license.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_license.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_license.html( $li );\n\n        // 创建缩略图\n        uploader_license.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_license, thumbnailHeight_license );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_license.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_license.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_license\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_license.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_license.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","account":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"account\" value=\"0\"  id=\"cover_id_account\"  />\n    <div id=\"fileList_account\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_account\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_account = $('#fileList_account'),\n        // 优化retina, 在retina下这个值是2\n        ratio_account = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_account = 100 * ratio_account,\n    thumbnailHeight_account = 100 * ratio_account,\n\n    // Web Uploader实例\n    uploader_account;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_account = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_account',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_account.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_account.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_account.html( $li );\n\n        // 创建缩略图\n        uploader_account.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_account, thumbnailHeight_account );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_account.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_account.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_account\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_account.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_account.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","prod_cert":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"prod_cert\" value=\"0\"  id=\"cover_id_prod_cert\"  />\n    <div id=\"fileList_prod_cert\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_prod_cert\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_prod_cert = $('#fileList_prod_cert'),\n        // 优化retina, 在retina下这个值是2\n        ratio_prod_cert = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_prod_cert = 100 * ratio_prod_cert,\n    thumbnailHeight_prod_cert = 100 * ratio_prod_cert,\n\n    // Web Uploader实例\n    uploader_prod_cert;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_prod_cert = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_prod_cert',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_prod_cert.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_prod_cert.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_prod_cert.html( $li );\n\n        // 创建缩略图\n        uploader_prod_cert.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_prod_cert, thumbnailHeight_prod_cert );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_prod_cert.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_prod_cert.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_prod_cert\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_prod_cert.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_prod_cert.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","registration":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"registration\" value=\"0\"  id=\"cover_id_registration\"  />\n    <div id=\"fileList_registration\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_registration\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_registration = $('#fileList_registration'),\n        // 优化retina, 在retina下这个值是2\n        ratio_registration = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_registration = 100 * ratio_registration,\n    thumbnailHeight_registration = 100 * ratio_registration,\n\n    // Web Uploader实例\n    uploader_registration;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_registration = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_registration',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_registration.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_registration.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_registration.html( $li );\n\n        // 创建缩略图\n        uploader_registration.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_registration, thumbnailHeight_registration );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_registration.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_registration.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_registration\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_registration.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_registration.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","online_sales":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"online_sales\" value=\"0\"  id=\"cover_id_online_sales\"  />\n    <div id=\"fileList_online_sales\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_online_sales\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_online_sales = $('#fileList_online_sales'),\n        // 优化retina, 在retina下这个值是2\n        ratio_online_sales = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_online_sales = 100 * ratio_online_sales,\n    thumbnailHeight_online_sales = 100 * ratio_online_sales,\n\n    // Web Uploader实例\n    uploader_online_sales;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_online_sales = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_online_sales',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_online_sales.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_online_sales.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_online_sales.html( $li );\n\n        // 创建缩略图\n        uploader_online_sales.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_online_sales, thumbnailHeight_online_sales );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_online_sales.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_online_sales.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_online_sales\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_online_sales.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_online_sales.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","legal_person_front":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"legal_person_front\" value=\"0\"  id=\"cover_id_legal_person_front\"  />\n    <div id=\"fileList_legal_person_front\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_legal_person_front\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_legal_person_front = $('#fileList_legal_person_front'),\n        // 优化retina, 在retina下这个值是2\n        ratio_legal_person_front = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_legal_person_front = 100 * ratio_legal_person_front,\n    thumbnailHeight_legal_person_front = 100 * ratio_legal_person_front,\n\n    // Web Uploader实例\n    uploader_legal_person_front;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_legal_person_front = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_legal_person_front',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_legal_person_front.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_legal_person_front.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_legal_person_front.html( $li );\n\n        // 创建缩略图\n        uploader_legal_person_front.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_legal_person_front, thumbnailHeight_legal_person_front );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_legal_person_front.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_legal_person_front.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_legal_person_front\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_legal_person_front.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_legal_person_front.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","legal_person_back":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"legal_person_back\" value=\"0\"  id=\"cover_id_legal_person_back\"  />\n    <div id=\"fileList_legal_person_back\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_legal_person_back\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_legal_person_back = $('#fileList_legal_person_back'),\n        // 优化retina, 在retina下这个值是2\n        ratio_legal_person_back = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_legal_person_back = 100 * ratio_legal_person_back,\n    thumbnailHeight_legal_person_back = 100 * ratio_legal_person_back,\n\n    // Web Uploader实例\n    uploader_legal_person_back;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_legal_person_back = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_legal_person_back',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"0\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_legal_person_back.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_legal_person_back.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_legal_person_back.html( $li );\n\n        // 创建缩略图\n        uploader_legal_person_back.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_legal_person_back, thumbnailHeight_legal_person_back );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_legal_person_back.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_legal_person_back.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_legal_person_back\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_legal_person_back.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_legal_person_back.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>"},"expire":1603612584343}